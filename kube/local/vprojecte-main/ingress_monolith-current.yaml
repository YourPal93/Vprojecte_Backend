---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: vprojecte-main
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: vprojecte-main
                port:
                  number: 80