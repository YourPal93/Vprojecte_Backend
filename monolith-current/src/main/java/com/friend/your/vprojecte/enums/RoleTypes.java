package com.friend.your.vprojecte.enums;

public enum RoleTypes {
    ROLE(0),
    GROUP_ROLE(1);

    public final int numericValue;

    RoleTypes(int numericValue) {
        this.numericValue = RoleTypes.this.numericValue;
    }
}
